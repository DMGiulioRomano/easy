composition:
  title: "il mondo dorme"

streams:
  - stream_id: "texture1"
    onset: 0.0
    duration: 10.0
    sample: "test.wav"
    # === GRAIN ===
    grain:
      duration: .03      
      envelope: half_sine
    # === DENSITY ===
    density: 50         
    distribution: 0
    # === POINTER (sample reading position) ===
    pointer:
      start: 0.0        
      mode: linear      
      speed: 0.3    
    # === PLAYBACK ===
    pitch:
      shift_semitones: 0
    # === SPATIAL ===
    output:
      pan: 0.5          
      volume: -6        
    # -----------------------------------------------------------------